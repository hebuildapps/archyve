{
  "name": "@archyve/extension",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "tsup && node scripts/package.mjs",
    "dev": "tsup --watch",
    "package": "npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
